type Query {
  me: User!
  organizations: [Organization]!
  organization(id: ID!): Organization
  project(id: ID!): Project
  stories(projectId: ID!): [Story]!
  story(projectId: ID!, id: ID!): Story
}