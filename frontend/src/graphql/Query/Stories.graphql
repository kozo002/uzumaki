query Stories($projectId: ID!) {
  stories(projectId: $projectId) {
    id
    title
    description
    state
    type
    points
    requester {
      id
      name
    }
    createdAt
    updatedAt
  }
}